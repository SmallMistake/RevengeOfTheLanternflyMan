using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Utils
{
    internal enum Teams { Player, Enemy }

    public enum DialogueFlags { Unassigned, Started, Completed, PostCompleted}
}
